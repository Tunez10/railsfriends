<nav class="fixed top-0 left-0 w-full bg-gray-900 text-white shadow-md z-50">
  <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between flex-wrap gap-y-4">
    
    <!-- Logo -->
    <div class="text-xl font-bold text-white">
      <%= link_to "Friend App", root_path, class: "hover:text-teal-400 transition duration-300" %>
    </div>

    <!-- Navigation Links -->
    <ul class="flex flex-wrap gap-6 text-sm font-medium">
      <li><%= link_to "Home", root_path, class: "hover:text-teal-400 transition duration-300" %></li>
      <li><%= link_to "About Us", home_about_path, class: "hover:text-teal-400 transition duration-300" %></li>
      <%# <li><%= link_to "Friends", friends_path, class: "hover:text-teal-400 transition duration-300"</li> %>
    </ul>

    <!-- Auth Links -->
    <ul class="flex flex-wrap gap-6 text-sm font-medium items-center">
      <% if user_signed_in? %>
        <li><%= link_to "Add Friend", new_friend_path, class: "hover:text-teal-400 transition duration-300" %></li>
        <li><%= link_to "Edit Profile", edit_user_registration_path, class: "hover:text-teal-400 transition duration-300" %></li>
        <li>
          <%= button_to "Sign out", destroy_user_session_path,
              method: :delete,
              class: "bg-red-600 hover:bg-red-700 px-4 py-1 rounded-md text-white transition duration-300" %>
        </li>
      <% else %>
        <li><%= link_to "Sign up", new_user_registration_path, class: "hover:text-teal-400 transition duration-300" %></li>
        <li><%= link_to "Sign in", new_user_session_path, class: "hover:text-teal-400 transition duration-300" %></li>
      <% end %>
    </ul>

    <!-- Search Bar -->
    <form class="flex gap-2 items-center">
      <input
        type="text"
        placeholder="Search..."
        class="px-3 py-1 rounded-md border border-gray-300 text-black bg-white focus:outline-none focus:ring-2 focus:ring-teal-500"
      />
      <button
        type="submit"
        class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-1 rounded-md transition duration-300"
      >
        Search
      </button>
    </form>

  </div>
</nav>

<!-- Add spacing below nav to prevent content from being hidden -->
<div class="h-[80px]"></div>
