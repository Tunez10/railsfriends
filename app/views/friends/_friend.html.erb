

  <tr id="<%= dom_id friend %>" class="text-sm odd:bg-gray-50 border-b border-gray-200 hover:bg-gray-100 transition-colors">
    <%# <td class="px-4 py-2 border border-gray-200">
      <%# <%= friend.first_name %>
      <%# <%= friend.last_name %> 
    <%# </td>  %>
    <td class="px-4 py-2 border border-gray-200"> 
     <%= link_to friend.first_name + ' ' + friend.last_name,  friend, class: "hover:text-blue-600 " %>

    </td>
    <td class="px-4 py-2 border border-gray-200"><%= friend.email %></td>
    <td class="px-4 py-2 border border-gray-200"><%= friend.phone %></td>
    <td class="px-4 py-2 border border-gray-200"><%= friend.twitter %></td>
    <%# <td class="px-4 py-2 border border-gray-200"><%= friend.user_id</td>  %>
    <td class="px-4 py-2 border border-gray-200">
      <div class="flex space-x-6 ">
        <%# <%= link_to "Show", friend, class: "text-blue-600 hover:underline "  %>
        <%# <%= link_to "Edit", edit_friend_path(friend), class: "text-blue-600 hover:underline" %> 
        <%= button_to "Destroy", friend, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "bg-red-600 px-2 py-1 text-white text-sm rounded-md hover:bg-red-700 hover:cursor-pointer transition duration-300 " %>
      </div>
    </td>
  </tr>



<%# <% if friend.user == current_user %> 
<%# <%= link_to "Edit", edit_friend_path(friend), class: "text-blue-600 hover:underline" %> 
<%# <%= button_to "Destroy", friend, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "text-red-600 hover:underline" %> 
<%# <% else %>
<%# <span class="text-gray-400">Edit</span> %>
<%# <span class="text-gray-400">Delete</span> %>
<%# <% end %> 
