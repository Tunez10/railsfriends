

<div class="md:w-2/3 w-full px-20 bg-gray-100 mt-10 mx-auto py-16 rounded-md shadow-lg">
 

  <%# <h1 class="font-bold text-4xl">Showing friend</h1> %>


  <div class="space-y-2">
    <div class="mt-4 flex gap-2 text-3xl">
      <p><%= @friend.first_name %></p>
      <p><%= @friend.last_name %></p>
    </div>

    <div class="flex sm:flex-row gap-1">
      <p><%= @friend.email %> |</p>
      <p><%= @friend.phone %> |</p>
      <p><%= @friend.twitter %> </p>
    </div>
  </div>

  <div class="mt-5 flex gap-2">
    <%= link_to "Edit", edit_friend_path(@friend), class: "w-full sm:w-auto text-center rounded-md px-2.5 py-1 text-sm bg-gray-300 hover:bg-gray-400 inline-block font-medium" %>
    <%= button_to "Destroy", @friend, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "bg-red-600 px-2 py-1 text-white text-sm rounded-md hover:bg-red-700 hover:cursor-pointer transition duration-300 " %>|
    <%= link_to "Back", friends_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-2.5 py-1 text-sm bg-gray-600 hover:bg-gray-700 text-white font-medium" %>
  </div>
 
</div>
